<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gdu.tagmusic.mapper.ChatMapper">
	
	
	<!-- 댓글 삽입하는 쿼리 -->
	<insert id="insertChat"  parameterType="ChatDTO">
		INSERT INTO CHAT_ADMIN
  		  (CHAT_NO, USER_NO, CHAT_DATE, IP, CONTENT, STATE, DEPTH, GROUP_NO, GROUP_ORDER)
		VALUES
   		 (CHAT_SEQ.NEXTVAL, #{userNo}, SYSDATE, #{ip}, #{content}, 1, 0, CHAT_SEQ.CURRVAL, 0)
	</insert>
	
	
	
	<!-- 로그인한 유저의 게시판만 조회하는 쿼리 -->
	<select id="selectChatList" parameterType="int" resultType="">
	
	
	
	
	</select>
	
	
	
	
	
	
	
	
	
	
</mapper>
